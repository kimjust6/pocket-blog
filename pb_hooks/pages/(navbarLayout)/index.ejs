<script server>
    const isHomepage = true
</script>

<section class="flex justify-center w-full">
    <div
        class="max-w-6xl relative isolate overflow-hidden glass-panel border border-white/10 bg-base-100/80 px-6 py-12 shadow-2xl shadow-primary/20"
    >
        <div class="relative gap-12 max-w-6xl">
            <div class="space-y-8 text-left pl-12 pr-8">
                <span class="glow-badge tracking-[0.3em]">Personal Blog</span>
                <h1 class="text-4xl font-semibold leading-tight sm:text-5xl">
                    <span class="text-gradient">
                        <%= (data.metadata.find(meta => meta.name ===
                        "og:site_name")).content %>
                    </span>
                </h1>
                <p class="text-3xl text-base-content/80 leading-relaxed">
                    Where I document the success stories, mistakes, and lessons
                    learned.
                </p>
                <div class="flex flex-wrap gap-4"></div>
            </div>
        </div>
    </div>
</section>

<%- include("blogcards.ejs", { isHomepage }) %>
